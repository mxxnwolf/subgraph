{
  "name": "tracer-test",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ bluemonstergames/tracer-test",
    "create-local": "graph create --node http://localhost:8020/ bluemonstergames/tracer-test",
    "remove-local": "graph remove --node http://localhost:8020/ bluemonstergames/tracer-test",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 bluemonstergames/tracer-test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.25.2",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
